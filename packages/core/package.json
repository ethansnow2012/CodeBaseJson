{
  "name": "core",
  "version": "0.1.0",
  "private": true,
  "description": "Core functionality for the project.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node ./index.ts",
    "test": "mocha -r ts-node/register -r tsconfig-paths/register './**/*.spec.ts'",
    "build": "npm run test && tsc",
    "paths-check": "tsc --traceResolution"
  },
  "dependencies": {
    "ignore": "^5.3.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "typescript": "^4.0.0",
    "mocha": "^8.0.0",
    "@types/mocha": "^8.0.0"
  }
}
